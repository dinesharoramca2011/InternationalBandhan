// confirmation.js
// Optional: Clear cart data after order is placed

document.addEventListener("DOMContentLoaded", () => {
  // Clear cart from localStorage
  localStorage.removeItem("cartItems");

  // Optional: You can show a summary from sessionStorage if needed
});
